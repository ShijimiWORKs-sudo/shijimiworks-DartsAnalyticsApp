# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Local data (SQLite DB, generated reports) - never committed
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Media (video/photo originals are user-device only, never in git)
media/

# Downloaded ML model weights (see models/README.md for setup) — binary,
# not source, and too large/version-specific to commit.
models/*.task
models/*.tflite

# OS
.DS_Store
Thumbs.db
